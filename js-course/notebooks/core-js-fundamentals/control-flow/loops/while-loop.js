const array = [1, 2, 3]; // Arrays were introduced in 2.1 and later in 2.5.1.

let index = 0; // Start at the first position of the array.

while (index < array.length) {
  console.log(array[index]); // Access the current element.
  index++; // Add 1 to move to the next position.
}
